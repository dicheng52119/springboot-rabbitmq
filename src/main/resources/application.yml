spring:
  application:
    name: springboot-rabbitmq
  # rabbitmq config
  rabbitmq:
    addresses: 127.0.0.1:5672
    username: guest
    password: guest
    publisher-confirms: true
    virtual-host: /